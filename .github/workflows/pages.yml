name: Deploy GitHub Pages

on:
  workflow_dispatch:   # 👉 chỉ build khi bạn bấm nút "Run workflow"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4   # ✅ update lên v4

      - name: Setup Pages
        uses: actions/configure-pages@v4   # ✅ update lên v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3   # ✅ update lên v3 mới nhất
        with:
          path: '.'   # 👉 nếu web tĩnh thì để thư mục gốc

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4   # ✅ update lên v4
